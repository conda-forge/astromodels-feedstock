{% set name = "astromodels" %}
{% set version = "2.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/astromodels-{{ version }}.tar.gz
  sha256: ab5ae01afb74b926aba38d0c379478fed27ef5decc65e1524d8d094f12dc8f3c

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  skip: true  # [win]
  skip: true  # [py<=310]

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
  host:
    - python
    - numpy
    - pip
    - setuptools
    - versioneer
  run:
    - llvmlite <0.46
    - versioneer
    - astropy >=1.2
    - python
    - numpy >=1.6
    - pyyaml >=5.1
    - scipy >=0.14
    - astropy-base >=1.2
    - dill
    - future
    - interpolation >=2.2.3
    - numba
    - h5py
    - pandas
    - pytables
    - colorama
    - omegaconf
    - rich
    - joblib

test:
  imports:
    - astromodels
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/threeml/astromodels
  summary: Astromodels contains models to be used in likelihood or Bayesian analysis in astronomy
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
